<h1>{{flower.name}}</h1>

{{#if flower.image}}
<img src="/images/{{flower.image}}">
{{else}}
  <p><i>Sorry, no image for {{flower.name}}</i></p>
{{/if}}

<p>{{flower.name}} is {{flower.color}}</p>

<p>Visit this flower's page at Wikipedia at the following link:
<a href="{{flower.wiki}}">{{flower.wiki}}</a></p>
